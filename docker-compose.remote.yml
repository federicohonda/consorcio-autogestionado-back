services:
  api:
    build: .
    ports:
      - "8001:8000"
    env_file:
      - .env
    dns:
      - 8.8.8.8
      - 8.8.4.4
    sysctls:
      net.ipv6.conf.all.disable_ipv6: 1

networks:
  default:
    driver: bridge
